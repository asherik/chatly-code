package com.chatlycode.agent.domain;

public enum AgentActionType {
    READ_FILE,
    LIST_FILES,
    GREP,
    GLOB,
    RUN_COMMAND,
    APPLY_PATCH,
    GRAPH_QUERY,
    GIT_STATUS,
    GIT_DIFF
}
